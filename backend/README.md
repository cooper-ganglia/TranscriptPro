# ProTranscript Backend
Cloud server for transcription.

## Setup
1. `python -m venv venv`
2. `source venv/bin/activate` (Mac) or `venv\Scripts\activate` (Windows)
3. `pip install -r requirements.txt`
4. `uvicorn app:app --reload`